{
  "tool": "verilator",
  "type": "issue",
  "issue_number": 6872,
  "issue_url": "https://github.com/verilator/verilator/issues/6872",
  "title": "crash when accessing aggregate type parameters within interfaces",
  "body": "I prepared a set of tests accessing aggregate type parameters within interfaces and parts of those parameters.\n\nFor now it is possible to access the entire aggregate type parameter withing the interface array, but accessing parts of the aggregate causes a crash. The workaround I currently use is to first create a `localparam` with the full aggregate, and next get an element of the aggregate.\n\nI tested the code on edaplayground with the following results:\n- link to tests https://www.edaplayground.com/x/eqaY (not sure if sharing will work),\n- only VCS is properly processing all the code,\n- Rivera Pro complaints about `ERROR VCP2985 \"Array and struct literals are allowed only in assignment like contexts.\" \"testbench.sv\"` but if I comment those (equality comparison with a struct literal on RHS) out the tests pass,\n- Questa has some issues with handling the type `byte`: `Error: (vsim-8348) An override for an untyped parameter ('/tb/t/the_sub/#extparam#0') must be integral or real. Assigned type is 'byte$[0:3]'` but if I comment out the code handling bytes it works,\n- Xcelium retuns many `Hierarchical name ('the_intf_port[0].FOO') not allowed within a constant expression`, this seems a more fundamental issue.\n\nThe testcase `test_regress/t/t_interface_array_parameter_aggregate_access.v`:\n```\n// DESCRIPTION: Verilator: Get agregate type parameter from array of interfaces\n//\n// This file ONLY is placed into the Public Domain, for any use,\n// without warranty, 2024 by Todd Strader\n// SPDX-License-Identifier: CC0-1.0\n\ntypedef struct {\n    int BAR_INT;\n    bit BAR_BIT;\n    byte BAR_ARRAY [0:3];\n} foo_t;\n\ninterface intf\n  #(parameter foo_t FOO = '{4, 1'b1, '{8'd1, 8'd2, 8'd4, 8'd8}})\n    (input wire clk,\n     input wire rst);\n    modport modp (input  clk, rst);\nendinterface\n\nmodule sub (intf.modp the_intf_port [4], intf.modp single_intf_port);\n    localparam foo_t intf_foo          = the_intf_port[0].FOO;\n    localparam int   intf_foo_bar_int  = the_intf_port[0].FOO.BAR_INT;\n    localparam bit   intf_foo_bar_bit  = the_intf_port[0].FOO.BAR_BIT;\n    localparam byte  intf_foo_bar_byte = the_intf_port[0].FOO.BAR_ARRAY[3];\n    localparam foo_t single_foo          = single_intf_port.FOO;\n    localparam int   single_foo_bar_int  = single_intf_port.FOO.BAR_INT;\n    localparam bit   single_foo_bar_bit  = single_intf_port.FOO.BAR_BIT;\n    localparam byte  single_foo_bar_byte = single_intf_port.FOO.BAR_ARRAY[3];\n\n    initial begin\n        if (intf_foo          != foo_t'{4, 1'b1, '{1, 2, 4, 8}}) $stop;\n        if (intf_foo_bar_int  != 4) $stop;\n        if (intf_foo_bar_bit  != 1'b1) $stop;\n        if (intf_foo_bar_byte != 8'd8) $stop;\n        if (single_foo          != foo_t'{4, 1'b1, '{1, 2, 4, 8}}) $stop;\n        if (single_foo_bar_int  != 4) $stop;\n        if (single_foo_bar_bit  != 1'b1) $stop;\n        if (single_foo_bar_byte != 8'd8) $stop;\n    end\nendmodule\n\nmodule t (\n    clk\n);\n    logic rst;\n    input clk;\n\n    intf the_intf [4] (.*);\n    intf single_intf (.*);\n\n    sub\n    the_sub (\n        .the_intf_port   (the_intf),\n        .single_intf_port(single_intf)\n    );\n\n   always @(posedge clk) begin\n      $write(\"*-* All Finished *-*\\n\");\n      $finish;\n   end\nendmodule\n```\n\nI also made a pull request.",
  "labels": [],
  "state": "closed",
  "created_at": "2025-12-29",
  "closed_at": "2026-03-30",
  "code_blocks": [
    "// DESCRIPTION: Verilator: Get agregate type parameter from array of interfaces\n//\n// This file ONLY is placed into the Public Domain, for any use,\n// without warranty, 2024 by Todd Strader\n// SPDX-License-Identifier: CC0-1.0\n\ntypedef struct {\n    int BAR_INT;\n    bit BAR_BIT;\n    byte BAR_ARRAY [0:3];\n} foo_t;\n\ninterface intf\n  #(parameter foo_t FOO = '{4, 1'b1, '{8'd1, 8'd2, 8'd4, 8'd8}})\n    (input wire clk,\n     input wire rst);\n    modport modp (input  clk, rst);\nendinterface\n\nmodule sub (intf.modp the_intf_port [4], intf.modp single_intf_port);\n    localparam foo_t intf_foo          = the_intf_port[0].FOO;\n    localparam int   intf_foo_bar_int  = the_intf_port[0].FOO.BAR_INT;\n    localparam bit   intf_foo_bar_bit  = the_intf_port[0].FOO.BAR_BIT;\n    localparam byte  intf_foo_bar_byte = the_intf_port[0].FOO.BAR_ARRAY[3];\n    localparam foo_t single_foo          = single_intf_port.FOO;\n    localparam int   single_foo_bar_int  = single_intf_port.FOO.BAR_INT;\n    localparam bit   single_foo_bar_bit  = single_intf_port.FOO.BAR_BIT;\n    localparam byte  single_foo_bar_byte = single_intf_port.FOO.BAR_ARRAY[3];\n\n    initial begin\n        if (intf_foo          != foo_t'{4, 1'b1, '{1, 2, 4, 8}}) $stop;\n        if (intf_foo_bar_int  != 4) $stop;\n        if (intf_foo_bar_bit  != 1'b1) $stop;\n        if (intf_foo_bar_byte != 8'd8) $stop;\n        if (single_foo          != foo_t'{4, 1'b1, '{1, 2, 4, 8}}) $stop;\n        if (single_foo_bar_int  != 4) $stop;\n        if (single_foo_bar_bit  != 1'b1) $stop;\n        if (single_foo_bar_byte != 8'd8) $stop;\n    end\nendmodule\n\nmodule t (\n    clk\n);\n    logic rst;\n    input clk;\n\n    intf the_intf [4] (.*);\n    intf single_intf (.*);\n\n    sub\n    the_sub (\n        .the_intf_port   (the_intf),\n        .single_intf_port(single_intf)\n    );\n\n   always @(posedge clk) begin\n      $write(\"*-* All Finished *-*\\n\");\n      $finish;\n   end\nendmodule\n"
  ],
  "linked_pr": null,
  "pr_files": null,
  "pr_test_files": null
}
