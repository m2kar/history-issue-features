{
  "tool": "verilator",
  "type": "issue",
  "issue_number": 7335,
  "issue_url": "https://github.com/verilator/verilator/issues/7335",
  "title": "Bug: no latch inferred detection if buried in some hierarchy",
  "body": "```verilog\nmodule leaf(\n    input sel,\n    output logic ready\n);\n    always @(sel)\n        if ((sel == 1))\n            ready = 1;\nendmodule\n\ninterface iface(input clk);\n    logic sel;\n    logic ready;\nendinterface\n\nclass C_noinst;\n    virtual iface v;\n    int idx = 0;\n\n    task t_noinst();\n        forever begin\n            @(posedge v.clk);\n            if (v.ready && v.sel) begin\n            end\n        end\n    endtask\nendclass\n\nmodule t;\n    logic clk;\n    iface i(.clk(clk));\n\n    leaf d(\n        .sel(i.sel),\n        .ready(i.ready)\n    );\n\n    initial #10 $finish;\nendmodule\n```\n\nWhat output from that test indicates it is wrong, and what is the correct or expected output?  (Or, please make test self-checking if possible.)\nThere is no `%Warning-LATCH: t/t_virtual_interface_unused_task_trigger.v:41:5: Latch inferred for signal 'ready' (not all control paths of combinational always assign a value)`, I expect there should be, it does appear when I move the always block directly into `t`.\n\nWhat 'verilator' command line do we use to run your example?\n`verilator --binary test.sv && obj_dir/Vtest`\n\nWhat 'verilator --version' are you using?  Did you try it with the git master version?  Did you try it with other simulators?\nGit master version.\n\nWhat OS and distribution are you using?\nArch\n\nMay we assist you in trying to fix this in Verilator yourself?\nI honestly have other things to do and don't really want to spend time on this one.",
  "labels": [
    "area: lint"
  ],
  "state": "open",
  "created_at": "2026-03-27",
  "closed_at": null,
  "code_blocks": [
    "module leaf(\n    input sel,\n    output logic ready\n);\n    always @(sel)\n        if ((sel == 1))\n            ready = 1;\nendmodule\n\ninterface iface(input clk);\n    logic sel;\n    logic ready;\nendinterface\n\nclass C_noinst;\n    virtual iface v;\n    int idx = 0;\n\n    task t_noinst();\n        forever begin\n            @(posedge v.clk);\n            if (v.ready && v.sel) begin\n            end\n        end\n    endtask\nendclass\n\nmodule t;\n    logic clk;\n    iface i(.clk(clk));\n\n    leaf d(\n        .sel(i.sel),\n        .ready(i.ready)\n    );\n\n    initial #10 $finish;\nendmodule\n"
  ],
  "linked_pr": null,
  "pr_files": null,
  "pr_test_files": null
}
