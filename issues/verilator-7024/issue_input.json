{
  "tool": "verilator",
  "type": "issue",
  "issue_number": 7024,
  "issue_url": "https://github.com/verilator/verilator/issues/7024",
  "title": "Multiplication operator unsupported inside constraint expressions",
  "body": "Following test unsupported cause Verilator miss the multiplication operation in smt format.\n\n\n```\n\n        // Test 1: Simple multiplication constraint\n        if (std::randomize(a, b, c) with { a == 5; b == 3; c == a * b; } != 1) $stop;\n        if (c != 15) $stop;\n\n        // Test 2: Multiplication with range constraints\n        if (std::randomize(x, y, z) with {\n            x >= 1; x <= 10;\n            y >= 1; y <= 10;\n            z == x * y;\n        } != 1) $stop;\n        if (z != x * y) $stop;\n\n        // Test 3: Multiplication with inside operator\n        if (std::randomize(x, y, z) with {\n            x inside {[2:5]};\n            y inside {[3:6]};\n            z == x * y;\n        } != 1) $stop;\n        if (z != x * y) $stop;\n```",
  "labels": [
    "area: randomization"
  ],
  "state": "closed",
  "created_at": "2026-02-09",
  "closed_at": "2026-02-09",
  "code_blocks": [
    "\n        // Test 1: Simple multiplication constraint\n        if (std::randomize(a, b, c) with { a == 5; b == 3; c == a * b; } != 1) $stop;\n        if (c != 15) $stop;\n\n        // Test 2: Multiplication with range constraints\n        if (std::randomize(x, y, z) with {\n            x >= 1; x <= 10;\n            y >= 1; y <= 10;\n            z == x * y;\n        } != 1) $stop;\n        if (z != x * y) $stop;\n\n        // Test 3: Multiplication with inside operator\n        if (std::randomize(x, y, z) with {\n            x inside {[2:5]};\n            y inside {[3:6]};\n            z == x * y;\n        } != 1) $stop;\n        if (z != x * y) $stop;\n"
  ],
  "linked_pr": null,
  "pr_files": null,
  "pr_test_files": null
}
