{
  "tool": "yosys",
  "type": "issue",
  "issue_number": 563,
  "issue_url": "https://github.com/YosysHQ/yosys/issues/563",
  "title": "non-ANSI module declaration with default port values??",
  "body": "frontends/verilog/verilog_parser.y contains the following code for non-ANSI module definitions:\r\n```\r\nmodule_arg:\r\n\tTOK_ID {\r\n\t\tif (ast_stack.back()->children.size() > 0 && ast_stack.back()->children.back()->type == AST_WIRE) {\r\n\t\t\tAstNode *node = ast_stack.back()->children.back()->clone();\r\n\t\t\tnode->str = *$1;\r\n\t\t\tnode->port_id = ++port_counter;\r\n\t\t\tast_stack.back()->children.push_back(node);\r\n\t\t} else {\r\n\t\t\tif (port_stubs.count(*$1) != 0)\r\n\t\t\t\tfrontend_verilog_yyerror(\"Duplicate module port `%s'.\", $1->c_str());\r\n\t\t\tport_stubs[*$1] = ++port_counter;\r\n\t\t}\r\n\t\tdelete $1;\r\n\t} module_arg_opt_assignment |\r\n```\r\n\r\nThe `module_arg_opt_assignment` rule is used for an options default port value.\r\n\r\nHowever, the IEEE 1800-2012 spec specifically says that default port values are to be used ONLY on ANSI-style module por declarations:\r\n\r\n> 23.2.2.4 Default port values\r\nA module declaration may specify a default value for each singular input port. These default values shall be\r\nconstant expressions evaluated in the scope of the module where they are defined, not in the scope of the\r\ninstantiating module.\r\nThe informal syntax to declare a default input port value in a module is as follows:\r\nmodule module_name (\r\n...,\r\n[ input ] [ type ] port_identifier = constant_expression,\r\n... ) ;\r\nDefaults can be specified only for input ports and only in ANSI style declarations. A default shall not be\r\nspecified for a port declared as interconnect (an interconnect port).\r\n\r\n\r\ndisabling the `module_arg_opt_assignment`  rule by using (notice how the entire rule is disabled by the aded opening comment):\r\n```\r\nmodule_arg_opt_assignment:\r\n\t/* '=' expr {\r\n\t\tif (ast_stack.back()->children.size() > 0 && ast_stack.back()->children.back()->type == AST_WIRE) {\r\n\t\t\tAstNode *wire = new AstNode(AST_IDENTIFIER);\r\n\t\t\twire->str = ast_stack.back()->children.back()->str;\r\n\t\t\tif (ast_stack.back()->children.back()->is_reg)\r\n\t\t\t\tast_stack.back()->children.push_back(new AstNode(AST_INITIAL, new AstNode(AST_BLOCK, new AstNode(AST_ASSIGN_LE, wire, $2))));\r\n\t\t\telse\r\n\t\t\t\tast_stack.back()->children.push_back(new AstNode(AST_ASSIGN, wire, $2));\r\n\t\t} else\r\n\t\t\tfrontend_verilog_yyerror(\"Syntax error.\");\r\n\t} |\r\n\t/* empty */;\r\n```\r\npasses compilation as well as passes all tests.\r\n\r\nWe can learn 2 things from here:\r\n1. default port values is not used on any test, even whith ANSI port initializers.\r\n2. dropping  `module_arg_opt_assignment` from the 1st part of the `module_arg` rule (e.g. the standalone TOK_ID clause that catches non ANSI port declarations) should be safe.\r\n\r\nCan I remove the default port value for the non-ANSI case?\r\n",
  "labels": [
    "enhancement"
  ],
  "state": "open",
  "created_at": "2018-06-07",
  "closed_at": null,
  "code_blocks": [
    "module_arg:\r\n\tTOK_ID {\r\n\t\tif (ast_stack.back()->children.size() > 0 && ast_stack.back()->children.back()->type == AST_WIRE) {\r\n\t\t\tAstNode *node = ast_stack.back()->children.back()->clone();\r\n\t\t\tnode->str = *$1;\r\n\t\t\tnode->port_id = ++port_counter;\r\n\t\t\tast_stack.back()->children.push_back(node);\r\n\t\t} else {\r\n\t\t\tif (port_stubs.count(*$1) != 0)\r\n\t\t\t\tfrontend_verilog_yyerror(\"Duplicate module port `%s'.\", $1->c_str());\r\n\t\t\tport_stubs[*$1] = ++port_counter;\r\n\t\t}\r\n\t\tdelete $1;\r\n\t} module_arg_opt_assignment |",
    "module_arg_opt_assignment:\r\n\t/* '=' expr {\r\n\t\tif (ast_stack.back()->children.size() > 0 && ast_stack.back()->children.back()->type == AST_WIRE) {\r\n\t\t\tAstNode *wire = new AstNode(AST_IDENTIFIER);\r\n\t\t\twire->str = ast_stack.back()->children.back()->str;\r\n\t\t\tif (ast_stack.back()->children.back()->is_reg)\r\n\t\t\t\tast_stack.back()->children.push_back(new AstNode(AST_INITIAL, new AstNode(AST_BLOCK, new AstNode(AST_ASSIGN_LE, wire, $2))));\r\n\t\t\telse\r\n\t\t\t\tast_stack.back()->children.push_back(new AstNode(AST_ASSIGN, wire, $2));\r\n\t\t} else\r\n\t\t\tfrontend_verilog_yyerror(\"Syntax error.\");\r\n\t} |\r\n\t/* empty */;"
  ],
  "linked_pr": null,
  "pr_files": null,
  "pr_test_files": null
}
