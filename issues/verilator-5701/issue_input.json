{
  "tool": "verilator",
  "type": "issue",
  "issue_number": 5701,
  "issue_url": "https://github.com/verilator/verilator/issues/5701",
  "title": "The simulation efficiency of Verilator varies significantly under different timescales. I would like to know the reason.",
  "body": "Dear Author,\r\n\r\nI have two questions regarding the efficiency of using Verilator and would like to seek your guidance:\r\n\r\nWhen the timescale of a simulation project is set to 1ns/100ps, Verilator performs very well. However, when the timescale is changed to 1ns/1ps, the simulation efficiency drops almost 100 times. Interestingly, this issue does not seem to occur with Modelsim. Could you explain the reason behind such a significant reduction in efficiency?\r\n\r\nI understand that Verilator, during simulation, performs predictive evaluations based on the signals in the sensitivity list. However, instead of placing this evaluation process in the value-update phase, it is implemented within a while loop and executed repeatedly within each time slice. This approach seems to differ somewhat from the description of simulation algorithms in the Verilog standard. Could you elaborate on the rationale behind this design choice?\r\n\r\nI look forward to your response and sincerely appreciate your guidance. Thank you very much.\r\n\r\nAttached below is the relevant description of the simulation algorithm from the 1800-2023 standard for reference:\r\n```\r\nexecute_simulation {\r\n      T = 0;\r\n      initialize the values of all nets and variables;\r\n      schedule all initialization events into time zero slot;\r\n      while (some time slot is nonempty) {\r\n                   move to the first nonempty time slot and set T;\r\n                  execute_time_slot (T);\r\n        }\r\n}\r\nexecute_time_slot {\r\n        execute_region (Preponed);\r\n        execute_region (Pre-Active); \r\n        while (any region in [Active ... Pre-Postponed] is nonempty) {\r\n                  while (any region in [Active ... Post-Observed] is nonempty) {\r\n                           execute_region (Active);\r\n                          R = first nonempty region in [Active ... Post-Observed];\r\n                          if (R is nonempty)\r\n                          move events in R to the Active region;\r\n                  }\r\n                 while (any region in [Reactive ... Post-Re-NBA] is nonempty) {\r\n                           execute_region (Reactive);\r\n                            R = first nonempty region in [Reactive ... Post-Re-NBA];\r\n                           if (R is nonempty)\r\n                             move events in R to the Reactive region;\r\n                             }\r\n                 if (all regions in [Active ... Post-Re-NBA] are empty)\r\n                            execute_region (Pre-Postponed);\r\n                            }\r\n            execute_region (Postponed);\r\n}\r\n**execute_region {\r\n          while (region is nonempty) {\r\n                     E = any event from region;\r\n                     remove E from the region;\r\n                     if (E is an update event) {\r\n                                 update the modified object;\r\n                                schedule evaluation event for any process sensitive to the object;\r\n                      } else { /* E is an evaluation event */\r\n                                 evaluate the process associated with the event and possibly\r\n                                schedule further events for execution;\r\n                                }\r\n           }\r\n}**\r\n```",
  "labels": [
    "resolution: answered"
  ],
  "state": "closed",
  "created_at": "2024-12-21",
  "closed_at": "2025-01-02",
  "code_blocks": [
    "execute_simulation {\r\n      T = 0;\r\n      initialize the values of all nets and variables;\r\n      schedule all initialization events into time zero slot;\r\n      while (some time slot is nonempty) {\r\n                   move to the first nonempty time slot and set T;\r\n                  execute_time_slot (T);\r\n        }\r\n}\r\nexecute_time_slot {\r\n        execute_region (Preponed);\r\n        execute_region (Pre-Active); \r\n        while (any region in [Active ... Pre-Postponed] is nonempty) {\r\n                  while (any region in [Active ... Post-Observed] is nonempty) {\r\n                           execute_region (Active);\r\n                          R = first nonempty region in [Active ... Post-Observed];\r\n                          if (R is nonempty)\r\n                          move events in R to the Active region;\r\n                  }\r\n                 while (any region in [Reactive ... Post-Re-NBA] is nonempty) {\r\n                           execute_region (Reactive);\r\n                            R = first nonempty region in [Reactive ... Post-Re-NBA];\r\n                           if (R is nonempty)\r\n                             move events in R to the Reactive region;\r\n                             }\r\n                 if (all regions in [Active ... Post-Re-NBA] are empty)\r\n                            execute_region (Pre-Postponed);\r\n                            }\r\n            execute_region (Postponed);\r\n}\r\n**execute_region {\r\n          while (region is nonempty) {\r\n                     E = any event from region;\r\n                     remove E from the region;\r\n                     if (E is an update event) {\r\n                                 update the modified object;\r\n                                schedule evaluation event for any process sensitive to the object;\r\n                      } else { /* E is an evaluation event */\r\n                                 evaluate the process associated with the event and possibly\r\n                                schedule further events for execution;\r\n                                }\r\n           }\r\n}**"
  ],
  "linked_pr": null,
  "pr_files": null,
  "pr_test_files": null
}
