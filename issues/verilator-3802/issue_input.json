{
  "tool": "verilator",
  "type": "pull_request",
  "issue_number": 3802,
  "issue_url": "https://github.com/verilator/verilator/pull/3802",
  "title": "Support unpacked structs",
  "body": "This is in fact a reopen of https://github.com/verilator/verilator/pull/3795. Apparently GH doesn't allow reopening PRs that had force pushes when closed.\r\n\r\nThe change adds support for unpacked structs and for `--structs-packed` flag.\r\n\r\nThe structs are transformed into regular C++ structs.\r\n\r\nThe implementation supports embedding dynamic structures inside structs, and assigning struct variables to each other.\r\n\r\nA parser-level warning on unpacked array declaration inside a struct has been replaced by a V3Width error on usages of all kinds of compound data inside packed structs -- detection of most of these wasn't implemented, which resulted in generation of invalid C++ code.\r\n \r\nWorth noting that number assignment to an unpacked struct passes Verilation, but produces C++ that doesn't compile, although I've figured out that this is the case also for dynamic arrays and class objects (see an [example](https://github.com/verilator/verilator/files/10135630/t_assign_const_to_object.zip)). Both probably should be addressed in a separate PR.\r\n\r\n`t_struct_unpacked2` test got deleted as it was meant to produce a warning on usage of unsupported unpacked structs. It's replaced by a similar test that produces a warning about unpacked union usage.\r\n\r\nThe main lacking feature is anonymous aggregate structure assignment (`'{...}`). For that reason `t_structu_dataType_assignment{,_bad}` test pair is weakened to test only packed structs as it in fact did before (with the `--structs-packed` flag).\r\n",
  "labels": [],
  "state": "closed",
  "created_at": "2022-12-07",
  "closed_at": "2022-12-21",
  "code_blocks": [],
  "linked_pr": null,
  "pr_files": null,
  "pr_test_files": null
}
