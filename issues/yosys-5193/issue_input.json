{
  "tool": "yosys",
  "type": "pull_request",
  "issue_number": 5193,
  "issue_url": "https://github.com/YosysHQ/yosys/pull/5193",
  "title": "verilog: fix parser \"if\" memory errors.",
  "body": "Fix buggy memory allocation introduced in #5152:\r\n\r\n1) clean up `ast_stack` to reflect AST node rearrangement when necessary, to avoid dangling pointer;\r\n2) call `free_attr()` on unused attribute list when no new syntax node is created, to avoid leaking it.\r\n\r\n_What are the reasons/motivation for this change?_\r\nTo fix the allocation bug discussed in #5152 and #5189.\r\n\r\nThis also assists #5180, which was blocked by this bug.\r\n\r\n_Explain how this is achieved._\r\nCommit 370d5871f4bd04d6ccd57457827dc1dfcaf74d1e handles an `else if` branch within a `unique if` by replacing the current `AST_COND`/`AST_DEFAULT`/`AST_BLOCK` \"else\" construction with a new `AST_COND`/(expr)/`AST_BLOCK` within the original `AST_CASE`.  However, it mistakenly left a dangling reference to the original (now orphaned) structure reachable via `ast_stack`.  This commit updates the `ast_stack` state to point to the replacement instead of the orphan.\r\n\r\n_If applicable, please suggest to reviewers how they can test the change._\r\nBuild with `clang` and `SANITIZER=address`.  Then `make test` (in particular `tests/verilog/unique_if_enc.ys`) should succeed.  (The test case in https://github.com/YosysHQ/yosys/pull/5152#issuecomment-2982806491 is a smaller example triggering the same logic.)",
  "labels": [],
  "state": "closed",
  "created_at": "2025-06-23",
  "closed_at": "2025-06-23",
  "code_blocks": [],
  "linked_pr": null,
  "pr_files": null,
  "pr_test_files": null
}
