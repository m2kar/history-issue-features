{
  "tool": "yosys",
  "issue_number": 3804,
  "title": "Exhaustive if statement generates latches",
  "features": [
    {
      "name": "exhaustive if-else chain with AND conditions in combinational always",
      "category": "data_model",
      "description": "Code should include a combinational always @(*) block with an if-else-if chain where each branch condition uses logical AND (&&) and the chain collectively covers all 2^N combinations of N conditions (exhaustive), but the tool incorrectly infers a latch",
      "snippet": "always @(*) begin\n  if (!select_1 && !select_0) out = in_0;\n  else if (!select_1 && select_0) out = in_1;\n  else if (select_1 && !select_0) out = in_2;\n  else if (select_1 && select_0) out = in_3;\nend",
      "tags": ["if_else", "combinational", "latch_inference", "exhaustive", "and_conditions"],
      "source_bug_id": "yosys-3804",
      "tool": "yosys",
      "issue_url": "https://github.com/YosysHQ/yosys/issues/3804",
      "construct_complexity": 2,
      "ub_type": null,
      "error_pattern": "wrong_output"
    }
  ]
}
